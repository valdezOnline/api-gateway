# Claude Configuration

## Environment Notes
- Use `a` instead of `php artisan` (Docker alias)
- Working in Docker containers on macOS

## Dev Notes
When asked to "make a dev note" or document findings:
- Create files in `docs/dev-notes/` directory
- Use descriptive filenames with date: `feature-name-YYYY-MM-DD.md` or `issue-description.md`
- Include: problem description, root cause, solution, verification steps, and files to monitor
- Use markdown format with clear sections and status indicators (✅/❌)
